import React from "react";
import SearchScreenContainer from "../components/search/SearchScreenContainer";
import ScreenModule from "../modules/ScreenModule";

const SearchScreen = () => {
  return (
    <ScreenModule>
      <SearchScreenContainer />
    </ScreenModule>
  );
};

export default SearchScreen;
